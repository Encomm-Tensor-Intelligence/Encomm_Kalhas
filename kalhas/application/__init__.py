"""Application (use-case) layer for the KALHAS kernel.

Kept intentionally minimal in Phase 0: one read-only use case that assembles
system metadata, plus runtime configuration resolution. Domain and simulation
use cases arrive with later phases.
"""
